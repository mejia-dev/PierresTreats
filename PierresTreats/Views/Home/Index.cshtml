@{
  Layout = "_Layout";
}

@using PierresTreats.Models;

@* <div class="bodyColumnA">

</div> *@

<div class="bodyColumnB">

  <h1>Welcome to Pierre's Treats</h1>
  <p>We serve the finest baked goods around! Check out what we currently have available.</p>
  <div id="homeTreatsAndFlavors">

    <div>
      <h3>@Html.ActionLink("Treats", "Index", "Treats")</h3>
      @if (Model["treats"].Length == 0)
      {
        <p>No treats have been added yet.</p>
      }
      <ul class="textAlignLeft">
        @foreach (Treat trt in Model["treats"])
        {
          <li>@Html.ActionLink(@trt.TreatName, "Details", "Treats", new { id = @trt.TreatId })</li>
        }
      </ul>
    </div>

    <div>
      <h3>@Html.ActionLink("Flavors", "Index", "Flavors")</h3>
      @if (Model["flavors"].Length == 0)
      {
        <p>No flavors have been added yet.</p>
      }
      <ul class="textAlignLeft">
        @foreach (Flavor flvr in Model["flavors"])
        {
          <li>@Html.ActionLink(@flvr.FlavorName, "Details", "Flavors", new { id = @flvr.FlavorId })</li>
        }
      </ul>

    </div>

  </div>



</div>